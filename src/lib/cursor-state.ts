/** Cursor position normalized to [-1, 1] within the hero canvas bounds,
 * shared between the DOM-side glow overlay and the R3F tilt rig. */
export const cursorState = {
  x: 0,
  y: 0,
  active: false,
}
